# dependencies & build output
node_modules/
dist/
.wrangler/

# local secrets (Google Maps API key)
.env
.env.local

# source participant PDFs (ASTC-published; regenerate data/ locally, don't redistribute)
assets/*.pdf

# local agent/editor tooling
.claude/
.agents/
skills-lock.json

# OS cruft
.DS_Store
